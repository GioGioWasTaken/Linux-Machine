local options = {
  ensure_installed = { "c", "lua", "vim", "vimdoc", "query", "python", "markdown", "java" },
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
